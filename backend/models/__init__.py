"""
Models Package

Contains Pydantic schemas for API request/response validation
and (future) SQLAlchemy ORM models for database entities.

- schemas.py  → Pydantic models for API contracts
- database.py → ORM models (planned, when DB layer is added)
"""
