"""
Middleware Package

Cross-cutting concerns that run on every request/response cycle:

- error_handler.py    → Global exception handlers for consistent error responses
- request_logger.py   → Structured request/response logging
- auth.py             → JWT authentication guard (planned)
"""
